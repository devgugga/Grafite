# Design Specs and Plans

`specs/` and `plans/` are historical architectural records, not current operational instructions. They document the decisions and milestones that led to the repository's current state.

Active, enforceable rules live in [`docs/domains/`](../domains/README.md). When a spec and a domain pack disagree, the domain pack wins and the spec is left untouched as a historical record.
